import { storiesOf } from "@storybook/react";
import React from "react";
import CircularProgressBar from "./CircularProgressBar";

storiesOf("ProgressBars", module).add("CircularProgressBar", () => (
  <CircularProgressBar />
));
